# Credits

**The Ruck** — developed by **Sydney Edwards**.

Source files under `client/src`, `client/tests`, `server/src`, `server/tests`, and `shared` include a `// Developed by Sydney Edwards` (or equivalent) line at the top where applicable. Markdown docs include a footer credit. Package manifests list `author: Sydney Edwards`.
