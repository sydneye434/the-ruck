{
  "name": "the-ruck",
  "version": "0.1.0",
  "private": true,
  "author": "Sydney Edwards",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "seed": "npm run seed --prefix server",
    "build": "npm -w shared run build && npm -w server run build && npm -w client run build",
    "typecheck": "npm -w shared run typecheck && npm -w server run typecheck && npm -w client run typecheck",
    "test": "npm test --workspaces --if-present",
    "test:coverage": "npm run test:coverage --workspaces --if-present",
    "screenshots:rasterize": "node docs/screenshots/rasterize.mjs",
    "screenshots:capture": "node docs/screenshots/capture.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "playwright": "^1.49.1",
    "sharp": "^0.33.5"
  }
}

